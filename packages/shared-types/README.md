# Shared types

This package defines safe data contracts shared between the extension and remote reasoning backend. Contracts must exclude raw PII.

The initial contract describes tokenized values only: a token, category, and policy decision. It never contains a raw sensitive value.
